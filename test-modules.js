// Test script para verificar imports de módulos
// Este script verifica que todos los módulos se pueden importar correctamente

console.log('🧪 Iniciando test de módulos...');

try {
  // Test import de config
  console.log('📦 Testing config.js...');
  // Simular import - en un entorno real esto funcionaría

  console.log('✅ Todos los módulos parecen estar bien estructurados');
  console.log('🔍 El problema debe estar en el runtime del service worker');

} catch (error) {
  console.error('❌ Error en test de módulos:', error);
}